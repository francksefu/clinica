<p style="color: green"><%= notice %></p>

<h1 class="text-center font">Avaliados</h1>

<div id="avaliados">
  <% @avaliados.each do |avaliado| %>
    <%= render avaliado %>
    <p>
     
    </p>
  <% end %>
</div>

<div class="row mb-3">
    <div class="col-md-5"> </div>
    <div class="col-md-2">
      <%= link_to "New avaliado", new_avaliado_path, class: 'btn btn-primary' %>
    </div>
    <div class="col-md-5"> </div>
</div>

